<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stock Prediction</title>
    <link rel="stylesheet" href="../public/predict.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            text-align: center;
            padding: 20px;
        }

        h2 {
            color: #333;
            margin-bottom: 20px;
        }

        form {
            background: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            display: inline-block;
            text-align: left;
        }

        label {
            font-size: 18px;
            color: #555;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
        }

        button {
            background-color: #28a745;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
            width: 100%;
        }

        button:hover {
            background-color: #218838;
        }

        h3 {
            color: #333;
            margin-top: 15px;
        }

        p {
            color: red;
            font-size: 18px;
        }

        @media (max-width: 600px) {
            form {
                width: 90%;
            }

            input, button {
                font-size: 14px;
            }
        }

    </style>
</head>
<body>
    <h2>Stock Investment Prediction</h2>
    
    <form action="/predict" method="POST">
        <label>Enter Investment Amount:</label>
        <input type="number" name="amount" required>
        <button type="submit">Predict</button>
    </form>

    <% if (prediction) { %>
        <h3>Investment: $<%= prediction.investment %></h3>
        <h3>Predicted Profit: $<%= prediction.predicted_profit %></h3>
        <h3>Predicted Loss: $<%= prediction.predicted_loss %></h3>
        <h3>Risk Level: <%= prediction.risk %></h3>
    <% } else if (error) { %>
        <p>Error: <%= error %></p>
    <% } %>
</body>
</html>
