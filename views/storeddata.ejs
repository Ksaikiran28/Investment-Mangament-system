<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stored Investment Data</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, #1c3668, #254f9a);
            color: #fff;
            font-family: 'Poppins', 'sans-serif';
            padding: 20px;
        }

        .dashboard-container {
            background-color: #ffffff;
            color: #333;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            max-width: 1100px;
            margin: 20px auto;
            animation: fadeIn 1.5s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        h2 {
            text-align: center;
            margin-bottom: 30px;
            font-size: 2.5rem;
            color: #007bff;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.4);
        }

        .total-investment h3 {
            font-size: 1.8rem;
            margin-bottom: 30px;
            color: #28a745;
        }

        .investment-table-container {
            margin-top: 20px;
            overflow-x: auto;
        }

        .investment-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
        }

        .investment-table th, 
        .investment-table td {
            padding: 15px;
            text-align: center;
            border: 1px solid #ddd;
        }

        .investment-table th {
            background-color: #007bff;
            color: white;
            text-transform: uppercase;
        }

        .investment-table td {
            background-color: #f9f9f9;
            color: #333;
        }

        .investment-table tr:hover {
            background-color: rgba(0, 123, 255, 0.1);
            cursor: pointer;
        }

        .buttons-container {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        .btn {
            font-size: 1.2rem;
            padding: 12px 25px;
            border-radius: 10px;
            transition: all 0.3s ease;
        }

        .btn-info {
            background-color: #17a2b8;
            color: white;
            border: none;
        }

        .btn-info:hover {
            background-color: #007bff;
            transform: scale(1.1);
        }

        .btn-warning {
            background-color: #ffc107;
            color: white;
            border: none;
        }

        .btn-warning:hover {
            background-color: #ff9f00;
            transform: scale(1.1);
        }
    </style>
</head>

<body>
    <div class="dashboard-container">
        <h2>Stored Investment Data</h2>

        <!-- Display Total Investment -->
        <div class="total-investment text-center">
            <h3>Total Investment: ₹<%= totalInvestment.toLocaleString() %></h3>
        </div>

        <!-- Investment Table -->
        <div class="investment-table-container">
            <table class="investment-table table table-striped">
                <thead>
                    <tr>
                        <th>Company Name</th>
                        <th>Shares</th>
                        <th>Price per Share</th>
                        <th>Amount</th>
                        <th>Sector</th>
                    </tr>
                </thead>
                <tbody>
                    <% investments.forEach((investment) => { %>
                        <tr>
                            <td><%= investment.company_name %></td>
                            <td><%= investment.shares.toLocaleString() %></td>
                            <td>₹<%= (Number(investment.price_per_share) || 0).toFixed(2) %></td>
                            <td>₹<%= (Number(investment.amount) || 0).toFixed(2) %></td>

                            <td><%= investment.sector %></td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <!-- Buttons for chart and back to dashboard -->
        <div class="buttons-container">
            <a href="/sector-pie-chart" class="btn btn-info">View Data in Pie Chart</a>
            <a href="/dashboard" class="btn btn-warning">Back to Dashboard</a>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
